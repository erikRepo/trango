# Usage

Not written yet — trango has no runnable UI at this point in development
(see `TODO.md`, Vaihe 1 is the current state: an empty Cargo workspace).

Once `cargo run -p trango` opens a window and accepts video/subtitle
arguments (from Vaihe 8 onward), this page will document installation
and command-line usage.
